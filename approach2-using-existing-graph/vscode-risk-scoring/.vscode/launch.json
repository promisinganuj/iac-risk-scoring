{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/approach2-using-existing-graph/vscode-risk-scoring"],
      "outFiles": ["${workspaceFolder}/approach2-using-existing-graph/vscode-risk-scoring/dist/**/*.js"]
    }
  ]
}
